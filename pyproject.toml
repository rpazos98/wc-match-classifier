[project]
name = "wc-match-classifier"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "numpy>=1.26",
    "scikit-learn>=1.4",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
]

[project.scripts]
wc-classify = "classifier.cli:run_cli"

[dependency-groups]
dev = ["pytest>=8"]
