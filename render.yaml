services:
  - type: web
    name: wc-match-classifier
    runtime: docker
    plan: free
    branch: main
    envVars:
      - key: CORS_ORIGINS
        value: https://rpazos98.github.io,http://localhost:5173
